$kind: http-request
url: "{{baseUrl}}/admin/dashboard"
method: GET
auth:
  type: bearer
  credentials:
    token: "{{adminToken}}"
scripts:
  - type: afterResponse
    code: pm.test('Revenue > 0',pm.response.json().revenue>0)
    language: text/javascript
order: 1000
